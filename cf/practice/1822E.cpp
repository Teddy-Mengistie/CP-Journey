#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    for (; t > 0; t--) {
        int n;
        cin>>n;
        char s[n];
        fgets(s, n+1, stdin);
        int swaps = 0;
        cout << swaps << "\n";
    }
    
    return 0;
}